# ─── Required ─────────────────────────────────────────────────────────────────

# Your bridge's public domain (must be HTTPS in production)
LOCAL_DOMAIN=https://klistr.example.com

# Your Nostr hex private key — keep this safe!
# This is your real identity; all bridged posts will be signed with this key.
NOSTR_PRIVATE_KEY=

# Your handle on this bridge (e.g. alice → alice@klistr.example.com)
# Defaults to the first 8 characters of your public key if not set.
NOSTR_USERNAME=

# ─── Nostr Relays ─────────────────────────────────────────────────────────────

# Primary Nostr relay for subscribing to and publishing events
NOSTR_RELAY=wss://relay.mostr.pub

# Optional: comma-separated list of additional read relays
# POOL_READ_RELAYS=wss://relay.damus.io,wss://nos.lol

# Optional: comma-separated list of write relays for publishing bridged events
# POOL_WRITE_RELAYS=wss://relay.damus.io,wss://nos.lol

# ─── Database ─────────────────────────────────────────────────────────────────

# SQLite (default — no external dependencies):
DATABASE_URL=klistr.db

# PostgreSQL (for larger deployments):
# DATABASE_URL=postgresql://user:password@localhost:5432/klistr

# ─── RSA Keys ─────────────────────────────────────────────────────────────────
# Keys are auto-generated if not present — no manual step needed!
RSA_PRIVATE_KEY_PATH=private.pem
RSA_PUBLIC_KEY_PATH=public.pem

# ─── Optional ─────────────────────────────────────────────────────────────────

# HTTP port (default: 8000)
PORT=8000

# Whether to sign outbound HTTP requests (recommended: true)
SIGN_FETCH=true

# External Nostr URL resolver (for njump.me links)
EXTERNAL_BASE_URL=https://njump.me

# Log level: info (default) or debug
LOG_LEVEL=info

# Lightning zap splitting (optional)
# ZAP_PUBKEY=<your-nostr-pubkey>
# ZAP_SPLIT=0.1
